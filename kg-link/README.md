# kg-link

`kg-link` 是法律关系抽取子项目，负责：

- 关系样本构建
- 高阶 LLM 辅助标注训练集
- DeepKE 训练与微调
- DeepKE 推理输出

它不负责主流水线编排，统一由 `kg-build` 的 `link` 阶段调用。

## 当前状态

当前已提供：

- 样本文件格式约定
- 预测结果格式约定
- 占位预测接口

后续将逐步接入：

- DeepKE 数据集格式转换
- 训练脚本
- 推理脚本
- 模型版本管理
